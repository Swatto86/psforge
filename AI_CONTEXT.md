# PSForge — AI context

## System Overview

PSForge is a Tauri 2 + React desktop PowerShell IDE (ISE-style) for editing, running, and debugging `.ps1` scripts with an integrated terminal, built-in AST parser diagnostics (optional PSScriptAnalyzer when installed), and TabExpansion2 IntelliSense. Default layout favors the terminal (~72% vertical space at `splitPosition: 28`).

## Tech Stack & Architecture

- **Frontend:** React 19, Vite, Monaco (`@monaco-editor/react`), Tailwind 4, xterm.js
- **Backend:** Rust (`src-tauri/`), Tauri commands for PowerShell host, settings, files
- **State:** `src/store.tsx` (React context + reducer), settings persisted via `load_settings` / `save_settings`

## Component Map

| Area | Path |
|------|------|
| Shell / shortcuts | `src/App.tsx` |
| Menu bar + workflow toolbar | `src/components/Toolbar.tsx` (File/Edit/View/Help menus; Paste + Run / Run / Stop) |
| AI assistant pane / providers | `src/components/AssistantPane.tsx`, `src/components/AiProviderBar.tsx`, `src-tauri/src/ai.rs`, `src-tauri/src/ai_cursor.rs`, `src-tauri/src/ai_codex.rs`, `src-tauri/src/ai_opencode.rs` (`disableAi` hides the in-app assistant; providers are Codex CLI / Cursor CLI / OpenCode CLI; OpenCode can use local Ollama models; Send attaches `collectDebugBundleMarkdown` as `debugBundle`) |
| Editor + paste sanitize | `src/components/EditorPane.tsx`, `src/sanitize-paste.ts` |
| Run helpers | `src/run-utils.ts`, `src/terminal-utils.ts`, `src/direct-run.ts` (saved-file F5) |
| Integrated terminal | `src/components/TerminalPane.tsx` (console tabs + `window.__psforge_terminal_*` bridge), `TerminalSession.tsx` (React shell), `src/terminal/console-session.ts` (PTY lifecycle), `session-readers.ts`, `output-pump.ts`, `missing-command-suggest.ts` |
| Terminal theme / WT sync | `src/terminal/windows-terminal-theme.ts`, `src/terminal/xterm-theme.ts`, `src-tauri/src/windows_terminal.rs` |
| Font presets / status bar | `src/font-presets.ts`, `src/components/FontQuickControls.tsx` |
| Terminal toolbar | `src/components/OutputPane.tsx` (`Clear` = wipe display then restart session, `Copy` = selection, `Script output` = last F5 stdout/stderr) |
| Diagnostics | `src-tauri/src/ps_analyze.rs` (built-in parser + optional PSSA); `src-tauri/src/ps_pssa_install.rs` (check/install for PS 5.1/7); `src/components/PssaInstallControls.tsx` |
| Fix Problem (AI) | `src/fix-problem.ts`, `src/fix-all-sequential.ts`, `src/editor-fix-problem.ts`, `src/components/ProblemsPane.tsx` (squiggle Fix + Reference **Fix This** / **Fix All** one-at-a-time loop; AI tab blank Fix uses the same path) |
| Welcome quick start | `src/components/WelcomePane.tsx` (paste, recent runs, re-run) |
| Scratch / project runner | `src/scratch-utils.ts`, `src/project-config.ts`, `src/run-dir-presets.ts` |
| Phase 3 dialogs | `src/components/ScratchRecoveryDialog.tsx`, `CloseScratchDialog.tsx`, `PssaRunGateDialog.tsx` |
| Assistant / debug | `src/assistant-mode.ts`, `src/debug-bundle.ts`, `src/paste-summary.ts`, `src/components/ToastStack.tsx` |
| Status bar | `src/components/StatusBar.tsx` (run-dir leftmost, last run / cursor on the right) |
| Settings | `src/components/SettingsPanel.tsx`, `src-tauri/src/settings.rs` |
| Types / defaults | `src/types.ts` |

## Data Flow — script runner

1. **Paste:** Ctrl+V sanitize; **Paste + Run** (`Ctrl+Shift+Alt+V`) → new script tab + new local console → clean → format → F5. Welcome paste uses the same path when run-after-paste is on.
2. **Scratch:** Untitled tabs auto-save to `%APPDATA%/PSForge/scratch/{tabId}.ps1`; orphans offered via `list_scratch_files` + `ScratchRecoveryDialog`; close via `CloseScratchDialog`.
3. **Run:** Saved and scratch-backed tabs: `& 'path'` in the current console; working dir / execution policy staged via `stage_terminal_run_prep` and applied silently by the terminal bootstrap PSReadLine hook (`direct-run.ts`, `terminal.rs`). Truly pathless buffers: `psrun` temp wrapper. `resolveExecutionWorkDir()` + optional override; `.psforge.json` via `findProjectConfig`; presets in `settings.runDirPresets`; PSSA gate uses `PssaRunGateDialog` when warn.
4. **Output:** `__psforge_copy_terminal_output` (full scrollback); `__psforge_copy_last_run_output` / **Script output** button copies stdout+stderr via OSC 633 capture in `src/run-output-capture.ts` (marker scrollback fallback). Clear / `clearOutputOnRun` wipe via `src/terminal/wipe-display.ts` then restart the PTY.
5. **Fonts:** `fontFamily` / `outputFontFamily` + sizes persist in Rust settings; `linkEditorOutputFonts` syncs family; status bar `FontQuickControls` + Settings presets.

## Primary user workflow

Human + AI loop: script generated externally → **Paste Clean + Format** (`Ctrl+Shift+Alt+V`) or Welcome paste → **F5** → ask on the AI tab (debug bundle attached automatically). **Assistant mode** (`assistant-mode.ts`), **paste summary** toasts (`ToastStack`, `sanitizePastedTextWithSummary`), **debug bundle** (`debug-bundle.ts`, `collectDebugBundleMarkdown`).

## Recent Context & Decisions

- **2026-09-01:** PowerShell selector listed one install three times (**1.4.49**). A mise-managed pwsh appears as `.../powershell/latest/pwsh`, the mise shim, and `~/.local/bin/powershell` — all symlinks onto one binary. `dedupe_ps_hosts` (`powershell.rs`) dedupes unix candidates by the file they canonicalize to and drops version-manager shims (their canonical target is not named `pwsh*`/`powershell*`); a shim survives only when nothing else is found. `get_ps_versions` reads `defaultPsVersion` and passes it as the preferred spelling so the saved host stays the listed one and the selector cannot go blank. Windows discovery unchanged.
- **2026-09-01:** Linux/Omarchy round two (**1.4.48**). The app now sets `WEBKIT_DISABLE_DMABUF_RENDERER=1` itself on Linux (`lib.rs`, before GTK init, an explicit value is left alone): WebKitGTK's DMA-BUF renderer aborts the web process under Hyprland/NVIDIA, so PSForge previously needed a wrapper script to launch at all. `<select>` gets `appearance: none` plus explicit `option` colours — WebKitGTK drew a native GTK combo box and ignored the theme, leaving the PowerShell host dropdown grey on white. `TerminalPane.tsx` (1508 lines) is split: `terminal/console-session.ts` owns the PTY lifecycle in plain TS, with `session-readers.ts`, `output-pump.ts`, `missing-command-suggest.ts`, `ps-highlight.ts`, `remote-console.ts`, and `components/TerminalSession.tsx` / `TerminalTabStrip.tsx` / `TerminalRemoteDialog.tsx`; the mutable fn-ref bag is gone and `installWindowBridge` derives bridge teardown from the installed keys. Testing the extracted detector found that the "is not recognized" pattern only matched Windows PowerShell's "as the name of", so module suggestions never fired on pwsh 7 — it now accepts both wordings.
- **2026-09-01:** Integrated terminal was unreadable on Linux/Wayland (Omarchy): the console mounted before its pane had a layout, so xterm reported its own 2x1 floor and the PTY spawned PowerShell in a **2-column** window. `startupPtyDims` (`terminal-utils.ts`) now fits first and falls back to 120x30 until the pane is measured; the ResizeObserver resizes from there. Also removed both hand-rolled cursor-position replies in `TerminalPane.tsx` — a fabricated `ESC[1;1R` on session start and a duplicate reply for every `ESC[6n` seen in PTY output. xterm.js answers DSR itself, so those were extra bytes on the input stream that echoed as literal `^[[1;1R` / `1;25R` and desynchronised PSReadLine. Windows never hit either path because ConPTY absorbs `ESC[6n` and lays the pane out before mount. Version **1.4.47**.
- **2026-08-30:** Launch beep was ConPTY treating OSC 633's BEL terminator as a console bell (Windows Terminal's OpenConsole does not). Bootstrap `PSForge-WriteOsc633` now ends markers with ST (`ESC \`). Capture already accepted both. Not the user profile. Version **1.4.46**.
- **2026-08-28:** Bug sweep **12** + compile-speed (**1.4.45**). Scratch/F5 save no longer clears dirty when the buffer moved on during the write. Fix All/Fix This only replace the Monaco buffer for the tab being fixed. Welcome paste reports a missing PowerShell host or empty clipboard. Sanitized Ctrl+V run waits for the store update. Terminal run prep uses `PSFORGE_PREP_PATH` (Linux/macOS cwd/policy actually apply). Analyzer failures show a Problems warning. Rust: `line-tables-only` + unpacked split-debuginfo in dev; release `codegen-units = 64`; CI skips incremental/debuginfo.
- **2026-08-26:** Sequential Fix All (**1.4.43**). Reference **Fix All** and AI-tab blank **Fix** walk diagnostics one at a time (`fix-all-sequential.ts`): highest-priority problem → single Fix This AI call → apply → re-analyze → next. Skips stubborn items, caps at 25 passes, Stop cancels after the current call. Avoids the old bulk request that timed out or overflowed on large Problem lists.
- **2026-08-26:** Terminal Clear + clear-on-run (**1.4.42**). Clear (toolbar / palette) wipes the xterm display (`term.reset` via `wipeTerminalDisplay`) before restarting PowerShell so the old prompt is not left above the new session. When `clearOutputOnRun` is on (Paste + Run / F5), the runner uses that same Clear path and waits for the new session before sending the script command — no more buffer-only clear that left leftover scrollback.
- **2026-08-25:** Terminal cwd restore after F5 (**1.4.41**). After a direct-run script finishes, the integrated terminal returns to the cwd it had before the run (saved when run prep applies `Set-Location`; restored in the bootstrap `prompt` hook before the next prompt renders).
- **2026-08-25:** Terminal run echo + menu hover (**1.4.41**). F5 on saved/scratch scripts no longer echoes `Set-Location` (or execution-policy setup) — prep is staged to `%APPDATA%/PSForge/pending-run-prep.json` and applied silently by the terminal bootstrap PSReadLine hook; the terminal shows only `& 'path.ps1'`. Menu dropdown items use accent-tinted hover so File/Edit/View/Help entries are visible when mousing over them.
- **2026-08-24:** Terminal **Script output** copy (**1.4.40**). Toolbar button (was "Last run") copies last F5 stdout/stderr only: `run-output-capture.ts` parses OSC 633 markers to drop prompts and strips echoed command line; debug bundle uses same path.
- **2026-08-24:** Runner + class `::new` diagnostics (**1.4.39**). F5 no longer runs stale disk when auto-save fails or is skipped (falls back to psrun with editor buffer). Analyzer flags invalid `[Type]::new(argCount)` for script-defined classes (e.g. `[Dog]::new('Rex', 7)` when Dog only has a parameterless ctor).
- **2026-08-24:** Diagnostics launch + class inheritance (**1.4.38**). `useEditorDiagnostics` in `App.tsx` runs `analyze_script` independent of Monaco mount (fixes Reference/squiggles never starting). Analyzer AST-walks `TypeDefinitionAst` for implicit derived ctor vs non-parameterless base (e.g. `class Dog : Pet` at line 67). EditorPane only paints markers.
- **2026-08-24:** Terminal prompt chrome + diagnostics race (**1.4.37**). Bootstrap captures `PSForgePriorPrompt` **after** deferred profile load so oh-my-posh / custom prompts render again with shell integration. Editor diagnostics use one scheduler (no requestId bump on tab switch); stale results dropped via active-tab guard; PSSA line-0 findings map to line 1 for squiggles; Monaco themes set explicit error/warning squiggle colours.
- **2026-08-24:** Open-time diagnostics reliability (**1.4.36**). Analyzer runs as soon as script + PowerShell host are ready (Monaco model not required for Reference → Problems). Squiggles paint when the model/problems update. Terminal always starts `-NoProfile`, loads profile after PSReadLine `InlineView` + suppresses PTY resize warnings — fixes `'WindowHeight' is not less than …'` on console start when profile used ListView.
- **2026-08-24:** Open-time diagnostics (**1.4.35**). Analyzer runs as soon as the editor mounts (and again when the PowerShell host is selected), not only after an edit. Waiting for Monaco/model/host no longer clears Problems (that race left Reference empty until typing).
- **2026-08-24:** Fix All batches + Problems **Fix This** + live Reference (**1.4.34**). Large Problem lists no longer fail with “AI request is too long”: Fix All prioritizes parse/errors, keeps the question under 8k chars, attaches the list as diagnostics (not a truncated debug bundle), and toasts remaining count. Right-click a Problem → **Fix This**. Editor diagnostics follow `activeTab.content` so Reference → Problems / the badge update after typing and after AI fixes (not only on keystrokes).
- **2026-08-24:** Editor diagnostics + short-terminal warning (**1.4.33**). Squiggles run when a tab opens / Monaco mounts (not only after typing). Analyzer JSON survives trailing Windows PowerShell CLIXML noise. PTY rows floor at 5; terminal bootstrap sets PSReadLine `InlineView` so short panes no longer print `'WindowHeight' is not less than …`.
- **2026-08-24:** OpenCode lists Zen + Go + other CLI providers (**1.4.32**). Model refresh calls `opencode models` (`ai_opencode_list.rs`) instead of only Ollama. Labels mark `opencode/…` as Zen and `opencode-go/…` as Go. Connect Zen/Go/ChatGPT in the OpenCode TUI (`/connect`) so they appear after Refresh.
- **2026-08-24:** Reference **Fix All** (**1.4.32**). Problems toolbar sends every diagnostic + debug bundle to the configured AI (`buildFixAllProblemsQuestion` / `applyAiFix`) and replaces the script in place. Shares the Fix Problem path used by squiggle right-click.
- **2026-08-24:** PSSA install + Fix Problem AI (**1.4.31**). In-app `check_psscriptanalyzer` / `install_psscriptanalyzer` for the selected host and all discovered hosts (PS 5.1 + 7, CurrentUser, TLS1.2/NuGet for 5.1). Settings auto-install when missing; Problems pane Install CTA. Editor right-click on a diagnostic squiggle → **Fix Problem (AI)** (`editor-fix-problem.ts`) sends mode `fix` and replaces the script in place.
- **2026-08-24:** Built-in editor diagnostics (**1.4.30**). Red squiggles no longer require `Install-Module PSScriptAnalyzer` — `analyze_script` always runs PowerShell `Parser::ParseInput` (`ps_analyze.rs`); PSSA findings are merged only when the module is present. Settings toggle relabeled “Show editor diagnostics”.
- **2026-08-24:** Terminal Clear/Copy (**1.4.29**). Clear restarts the PowerShell session so prompt/Nerd Font chrome redraws (no blank buffer). Copy copies the **selection** only (`Ctrl+Shift+C` / right-click when selected); Last run still copies F5 output. Right-click: copy if selected, else paste.
- **2026-08-24:** Runner bulletproofing (**1.4.28**). Untitled/scratch F5 `&`s the on-disk scratch file in the live console (same as saved scripts). Named `-Param` tokens stay bare for binder matching. Temp-wrapper fallback uses call operator (`&`) not dot-source. Fixture `fixtures/runner-coverage.ps1` + live `pwsh` invoke-wrapper test locks in classes/ternary/pipelines/JSON/XML/try-catch. Edit → **Copy Selection to Terminal** (`Ctrl+Shift+Enter`). AI providers: **Codex CLI**, **Cursor CLI**, **OpenCode CLI** only.
- **2026-08-24:** Console chrome (**1.4.27**). Dropped the cyan `PSForge Terminal` line on session start so the shell prompt is the first thing shown. Moved the status-bar **Run:** / **Pinned:** path to the far left (no 220px cap) so the full working directory is visible; last-run / updates / Ln Col stay on the right.
- **2026-08-24:** Removed **Copy for AI** (**1.4.26**). Ask / Write / Fix Send attaches `collectDebugBundleMarkdown` (script, last run, PSSA, working dir, terminal output) as `debugBundle`; the backend prefers that over the separate script/diagnostics/terminal fields. Palette and status-bar last-run no longer copy a bundle for external chats. Copy Last Run / Copy Output remain.
- **2026-08-24:** OpenCode is a first-class AI provider (**1.4.25**). Settings and the AI tab can pick provider/model. OpenCode CLI (`opencode run --format json --model … --auto`) uses local Ollama models (`ollama/<tag>`), discovered via `GET /api/tags` on a loopback URL. Provider/model changes persist through the existing settings save debounce.
- **2026-08-24:** Open-and-run like VS Code (**1.4.24**). F5 on a saved `.ps1` invokes `& 'path'` in the current console after `Set-Location` (no temp wrapper / `-NoProfile` child). Mandatory params prompt in the terminal. Untitled/scratch still uses `psrun`. Consoles load the PowerShell profile by default. Welcome leads with Open script.
- **2026-08-24:** Integrated consoles honor Windows Terminal appearance (**1.4.23**). `read_windows_terminal_settings` loads Terminal `settings.json`; xterm uses that colour scheme (e.g. Tokyo Night) and font face (e.g. JetBrainsMono Nerd Font). Fallback is PSForge CSS theme + glyph-capable Nerd Font stack. PTY sets `COLORTERM=truecolor`. Compile-speed defaults: `[profile.dev] debug = "line-tables-only"` + `split-debuginfo = "unpacked"`; `[profile.dev.package."*"] opt-level = 1`; `[profile.release] codegen-units = 64` (no LTO). CI sccache + rust-cache + `CARGO_INCREMENTAL=0` / `CARGO_PROFILE_DEV_DEBUG=0`. Cargo 1.96 rejects `jobs = 0` so that key is omitted. Local Windows NSIS timed build (1.4.22): **157.1s** wall clock → `PSForge_1.4.22_x64-setup.exe` (unsigned local, updater artifacts off).
- **2026-08-23:** UI simplify + PS 5/7 hardening (**1.4.22**). Welcome is brand-first with one primary paste CTA; toolbar drops duplicate Modules/Settings icons; status bar keeps only live run context (path, run status, run dir, cursor); bottom pane drops title/subtitle/chip chrome and densifies tabs/actions (no pills). Fixed v1.4.21 release compile break (`concat!` → `format!` in `ps_invoke.rs`). PS 5.1 SecureString via NetworkCredential; pending psrun wrapper written with UTF-8 BOM; param inspect returns `{status,parameters}` so empty `param()` runs and inspect failures are distinct; CmdletBinding-aware param heuristic; space-form arg coerce; switch type in ParamPromptDialog; debug preflight parity with F5.
- **2026-08-23:** psrun / terminal hardening — the integrated terminal run path now shares the same argument tokenizer/coercer as the persistent host (`src-tauri/src/ps_invoke.rs`): a child invoke wrapper dot-sources the user script with a parsed named/positional splat instead of splatting raw strings onto `pwsh -File` (mis-bound switches/colon tokens). `get_script_parameters` reads `$ast.ParamBlock` only (no recursive Find that picked function param blocks and caused spurious ParamPromptDialog). When a script-level `param()` exists but inspection returns nothing, F5 is blocked with a terminal notice instead of falling through to an interactive console prompt. Multi-console black-screen fix: xterm WebGL disabled (WebView2 blank-webview regression when opening "+ Local"); new local tabs reveal the terminal pane and double-RAF fit/refresh. Version **1.4.21**.
- **2026-07-10:** Bug sweep **11** — the command palette's "Paste Clean + Format" bypassed the Welcome-tab fallback (silent no-op from the Welcome page); toolbar, Ctrl+Shift+Alt+V, and the palette now share one `pasteScriptFromClipboard` entry point in App.tsx that owns the welcome/new-script branch. Dead `AnsiText` helper removed from OutputPane. Version **1.4.19**.
- **2026-07-10:** Bug sweep **10** — Paste Clean + Format into an open tab had silently no-oped since the feature shipped: the `__psforge_insertTextAtSelection` / `__psforge_getEditorText` bridges were consumed and cleaned up but never registered; EditorPane now registers both. Recent-files merges and project-config application moved reducer-side (`MERGE_RECENT_FILES` / `APPLY_PROJECT_CONFIG`) so multi-file opens can't clobber each other via stale settings closures (S6-2 class). The re-run working-dir override is consumed before `runScript`'s guards (no leak into later runs), and re-run aborts with a notice when the recorded script can't be opened instead of running the active tab. Version **1.4.18**.
- **2026-07-10:** Single-instance guard: `tauri-plugin-single-instance` is registered first in the builder, so a second launch focuses/restores the running window instead of starting a new process. A file argument on the second launch (file-association double-click) is forwarded via a `psforge-open-path` event that App.tsx routes through `openFile` (gated on `settingsLoaded`, like the launch-path effect). `get_launch_path`'s argv parsing extracted into shared `launch_path_from_args`. Version **1.4.17**.
- **2026-07-10:** Close-button fix: Tauri's JS `onCloseRequested` wrapper destroys the window unless the handler calls `preventDefault()` — the settings-flush listener didn't, so pressing X destroyed the (already hidden) window and exited the app, dropping the tray icon. The handler now prevents before flushing; `core:window:allow-destroy`/`allow-close` capabilities removed so no frontend path can destroy the main window. Version **1.4.16**.
- **2026-07-10:** Tray lifecycle hardening (regression sweep). Exit (tray menu and File > Exit) now emits `psforge-exit-requested`; the store flushes debounced settings then calls plugin-process `exit(0)`, with a 2s backend force-exit watchdog for a dead webview. File > Exit previously called `window.close()`, which after the tray change only hid the window. macOS: `RunEvent::Reopen` (Dock click) restores the hidden window. Linux: appindicators emit no click events — the tray menu's Show item is the restore path; missing bundle icon now logs a warning. Versions **1.4.14–1.4.15**.
- **2026-07-10:** Added a system tray lifecycle: closing the main window hides PSForge, clicking the tray icon or Show restores it, and tray Exit fully terminates the application. Version **1.4.13**.
- **2026-07-10:** Bug sweep **9** — tab-bar batch closes now use the scratch-aware Save/Keep/Discard lifecycle, including permission to replace the final tab; Close All creates a fresh tab id so old per-tab diagnostics/markers cannot leak. Cancelling scratch Save As preserves the pending recovery write. Scratch path matching now follows OS case/separator semantics. Modal and close-icon accessible names completed. Version **1.4.12**.
- **2026-07-09:** **Explain Selection (AI)**. New Monaco action `psforge-explain-selection` (`Ctrl+Shift+E`, right-click menu, precondition `editorHasSelection && psforgeAiEnabled` — a context key synced to `disableAi`): sends the selection through the existing `ask_ai` mode-"ask" bridge (no backend changes; selection embedded in the question capped at 6k chars with backend-mirrored fence logic, full script as context) and shows the answer in a popover anchored below the selection (`ExplainPopover.tsx`, `.menu-pop` styling, Esc/click-outside close). "Open in Assistant" hands the question+answer to `AssistantPane` via a module-level handoff in `src/explain-selection.ts` (mount-read + `psforge-explain-handoff` event, covering both mounted and lazily-mounting pane). Stale responses guarded by a request-id ref; popover closes on tab switch and when AI is disabled. Version **1.4.9**.
- **2026-07-09:** Run-directory quick picker + Settings UI rework. The status-bar run-CWD entry is now a popup menu (was a pin/unpin toggle): use file's folder, pin current, choose folder (native dialog), and apply any run-dir preset (`StatusBar.tsx`, reuses `.menu-pop`/`.menu-item`/`.menu-header`). Settings panel reworked: rows are two-column (label + always-visible description on the left, control on the right — the hover-only "i" tooltip bubble is gone; `SettingRow` renders `tooltip` as text), panel enlarged to 960×660 (viewport-clamped), and a new **Run Directory** section holds Working Directory + Run Directory Presets (split out of the overloaded Execution section; Execution keeps Execution Policy). Version **1.4.8**.
- **2026-07-09:** Bug sweep **8** — user-facing pass. Stop routes Ctrl+C to the run console tab (not the visible one); save/open/format failures write terminal notices; scratch recovery uses `Untitled-N` + dirty; `selectDebugFrame` uses live pause refs. Version **1.4.6**.
- **2026-07-09:** Bug sweep **7** after the execution-actions extract. Fixed debug pause refs being wiped by render-time state mirroring (Continue/step no-op after break), debug completion always clearing run state + recording last-run exit code, F8 selection recording last-run, and pinned working-dir fallback parity with F5. Version **1.4.5**.
- **2026-07-08:** Native context menu removed. The WebView's browser right-click menu (Back/Reload/Inspect…) is suppressed app-wide (`main.tsx` document-level `contextmenu` preventDefault — Monaco's and the tab bar's custom menus are unaffected since they don't use the native default). Terminal right-click now **pastes** (classic PowerShell console behavior, `xterm-setup.ts` — `term.paste()` of `navigator.clipboard.readText()`), replacing the native menu that was previously the terminal's only mouse paste path. Version **1.4.3**.
- **2026-07-08:** Multi-agent bug sweep **6** — full-repo, then four adversarially-verified fix/verify rounds (23 confirmed findings, all fixed — see AUDIT-FIXES.md "Sweep 6"). Round 4 refinements: run-output reads (Copy Last Run / debug bundle / AI context) use a dedicated `get_run_content` bridge (`getRunTerminalPlainContent`) covering the marker-evicted fallback and returning "" once the run's tab is closed; the debug prompt regex is line-anchored (`/\[DBG\]:.*>>$/`). Round 3 additions: debug prompt detection anchored to `[DBG]: …>>` (script output can no longer fake a pause), run-output bridges (Copy Last Run / debug bundle / AI context) bound to the console tab that ran the script (`lastRunTabIdRef`), lossy-save baselines re-read from disk so the S6-10 conflict prompt can't flag PSForge's own save, the debug auto-save gained the same conflict guard, continue/step share a ref-synced `sendDebugCommand`, and a Rust `RunEvent::Exit` handler kills a mid-script persistent host on app close (`kill_active_run_on_exit`). Notable behavior changes: scratch auto-save now keeps protecting scratch-backed tabs instead of one-shotting after the first save, and scratch backing paths are treated as *unsaved* everywhere (Ctrl+S/Save All prompt Save As, Toolbar gates use the shared predicate, `scratchDir` exposed in app state); saving over a file that changed on disk outside PSForge now prompts before overwriting (`fileChangedOnDisk`), and auto-save-on-run skips with a terminal notice instead; run history records the tab that actually ran via a new delta `APPEND_RUN_RECORD` reducer action (settings changed mid-run no longer revert on completion); Assistant responses are bound to the tab they were generated for (`codeTabId`); debounced settings saves flush on window close (`onCloseRequested` + `core:window:allow-destroy`/`allow-close` capabilities); debugger Locals/Call Stack/Watches now auto-populate on break (ref-based pause guards); all ~14 ad-hoc PS helper spawns force UTF-8 console output via `ps_utf8_script` (was OEM CP850 mojibake); UTF-32 BOMs detected and converted with a warning instead of silently corrupting as UTF-16; terminal sessions registered before the PTY reader thread spawns. Version **1.4.2**.
- **2026-07-08:** AI-surface refinement after user feedback on 1.4.0. Removed the toolbar "AI" button (duplicate of the always-visible bottom "AI" tab — same `SET_BOTTOM_TAB` dispatch). "Copy for AI" is defined as an **external-AI clipboard workflow**, independent of the in-app assistant: it keeps its name and stays visible even when `disableAi` is on (conditional relabel and WelcomePane conditional reverted; Settings copy clarified). `disableAi` now means "no AI requests leave PSForge": hides the AI tab, hides provider settings, backend `ask_ai` refuses. Version **1.4.1**.
- **2026-07-08:** UI rework + AI kill-switch. Toolbar rebuilt around a classic **File / Edit / View / Help menu bar** (`Toolbar.tsx`): replaces the Recent dropdown, Open/Save icon buttons, the "⋯" overflow menu, the toolbar theme `<select>`, and the About/"?" icon buttons; every action now has a labeled menu item with its shortcut shown (File also lists up to 10 recent scripts; new `onOpenFolder`/`onCheckForUpdates` props from App). Primary workflow buttons unchanged. New **`disableAi`/`disable_ai` master setting** (Zed-style, Settings → AI): hides the AI bottom tab (falls back to Terminal via an `OutputPane` effect), hides the toolbar AI button, relabels "Copy for AI" → "Copy Debug Bundle", hides provider settings, and `ask_ai` refuses with `AI_DISABLED`. Settings panel: IntelliSense section merged into Editor. New menu CSS (`.menubar-btn`, `.menu-item-row`, `.menu-shortcut`, `.menu-header`, `.menu-check`). Version **1.4.0**.
- **2026-07-08:** User-facing bug sweep + regression sweep. Fixed stale release surfaces by adding a release-metadata test that keeps `package.json`, `package-lock.json`, Tauri config, Cargo metadata, Cargo lock, and README current-version link in sync; About now falls back to `unknown` instead of an old hard-coded version and uses cross-platform copy; Settings file-association context-menu toggle no longer reports the requested state when registration and status refresh both fail. Version **1.3.5**.
- **2026-07-07:** Multi-agent bug sweep **3** (32 confirmed findings, all fixed — see AUDIT-FIXES.md "Sweep 3"), then a regression sweep over the diff. Notable behavior changes: completion markers now bypass the output-line budget so a >100k-line run can't hang `execute()` (S3-1); `show_debugger_tools` is now a real backend setting + Settings toggle, and the Debugger panel shows whenever a debug session is live regardless of the toggle (S3-2); `.ps1` temp files carry a UTF-8 BOM so Windows PowerShell 5.1 doesn't mis-decode non-ASCII source (S3-6); "Copy Output" copies the full scrollback (was last 80 lines) and "Copy Last Run Output"/debug bundle use a reflow/eviction-safe xterm marker instead of a raw buffer index (S3-4/S3-13, `onStartLine`/`lastRunOutputStartLineRef` removed, new `__psforge_terminal_get_run_output_line_count` bridge); debug wrapper clears prior-run PSForge breakpoints before registering (S3-11); scratch auto-save no longer overwrites the "Untitled-N" tab title and TabBar hides the `<tab.id>.ps1` backing path (S3-17); `release.yml` sets `includeUpdaterJson: true` on every matrix leg so macOS/Linux auto-update works (S3-5). Version **1.3.1**.
- **2026-07-07:** Multi-agent bug sweep (21 confirmed findings, all fixed — see AUDIT-FIXES.md "Sweep 2") + UI overhaul for the AI paste-and-run loop. Toolbar now has labeled workflow buttons (Paste + Run / Run / Stop / Copy for AI) with shared CSS classes in styles.css (`.tb-action*`, `.icon-btn`, `.menu-pop`/`.menu-item`, `.tab-item*`, `.btn*`, `.status-link`, `.welcome-*`; new `--success` token per theme); TabBar/StatusBar/WelcomePane/PS7 banner restyled to match the bottom-pane pill language; Welcome pane leads with paste + a 3-step loop explainer; Ctrl+Shift+Alt+V now works from the Welcome tab (routes to paste-as-new-script). Notable behavior changes: tab ids are now `tab-<uuid>` (scratch filenames unchanged in format), run completion waits for stdout+stderr `PSFORGE_DONE` markers, `save_file_content` returns an optional lossy-encoding warning, run dir in "file" mode prefers the active tab's own folder, backend setting defaults now match the frontend (`autoSaveOnRun: true`, `sidebarVisible: false`), CI gained a windows-latest clippy+test job. Version **1.3.0**.
- **2026-06-09:** Follow-up fix found via empirical PowerShell 7 testing: `MODULE_SUGGEST_SCRIPT` (terminal "command not found → install module" hints) piped into a multi-line `Select-Object` whose first argument sat on the next line with no continuation, so PowerShell raised "Missing closing ')'" and both discovery branches always threw — suggestions never appeared. Put `Select-Object` + first arg on one line and end each line with the pipe. Version **1.2.18**.
- **2026-06-09:** Bug-sweep release: debugger inspector markers (`Write-Host ('<prefix>' + <json>)` — bare `+` was emitted as a separate argument so Locals/Call Stack/Watch never parsed), `$?` exclusion regex in the variables snapshot, `delete_scratch_file` path-traversal hardening (strict filename + parent-dir check), stale-cert guard in ScriptSigningDialog, ToastStack timer cleanup, last-tab context-menu Close disabled, CI now runs vitest, release workflow checks Cargo.toml version + installs pkg-config. Version **1.2.17**.
- **2026-06-01:** Optional "Open with PSForge" right-click menu (`register_context_menu`/`unregister_context_menu`/`get_context_menu_status`, Settings → File Associations toggle); status-bar run-dir link now toggles pin/unpin; file-association registration writes a proper `Applications\<exe>` Open With entry (FriendlyAppName + icon + command + SupportedTypes) and a ProgID FriendlyAppName to fix the broken/iconless Open With entry. Version **1.2.16**.
- **2026-05-25:** Phase 4 assistant workflow: `assistantMode` setting, paste summary toasts, Copy Debug Bundle, `ToastStack`. Version **1.2.15**.
- **2026-05-25:** README rewritten for AI paste-and-run workflow, releases link, shortcut table.
- **2026-05-25:** Phase 3 script-runner: scratch recovery dialog, untitled close (save/keep/discard), recent-run re-run/open folder/clear, `.psforge.json` + run-dir presets, in-app PSSA warn dialog, copy last run output, Vitest for `sanitize-paste`/`run-utils`. Version **1.2.14**.
- **2026-05-24:** Phase 2 script-runner: scratch folder, copy output, welcome paste, PSSA run gate, pinned run dir, recent runs log; editor/terminal font presets with persistence and status bar quick control. Version **1.2.13**.
- **2026-05-24:** `src-tauri/rust-toolchain.toml` pins **stable** + rustfmt/clippy (matches CI; fixes edition2024/zbus_names on older default Rust). Local `./scripts/ci-local.sh` uses it automatically.
